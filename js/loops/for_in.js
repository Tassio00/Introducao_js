
 // loop for-in

const pessoa4 ={
    nome: 'Tassio',
    idade: 19
};
// key-value

for(let chave in pessoa4){
    console.log(chave, pessoa4['nome']);
}

const cores = ['rosa', 'preto', 'verde'];

for(let indice in cores){
    console.log(cores[indice]);

}
